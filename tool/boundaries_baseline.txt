# Known feature-boundary violations, grandfathered when tool/check_boundaries.sh
# was repaired (it had grepped a pre-rebrand package prefix since the rename, so
# passed vacuously — these were never actually caught).
#
# Format: <importing file> <imported package uri>
# Anything not listed here fails the build. Burn this list down; do not add.
#
# EMPTY, as of M6. All seven entries had the same root cause: `settings_cubit`,
# `targets_cubit` and `block_app_tile` are cross-cutting surfaces that six
# features need but which were only reachable through blocking/**/presentation/.
# Exporting them from lib/features/blocking/blocking.dart — the content_counter
# and limits precedent — cleared the list in one edit. Keep it empty.
